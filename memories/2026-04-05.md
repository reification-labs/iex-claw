# 2026-04-05 — IExClaw Daily Log (Evening Session)

## Session: 6:17 PM – 8:48 PM ET — The Framework Emerges + Code Self-Surgery

### Substrate Completion (steps 1-3 of morning handoff)
- Extracted `IExClaw.Tools.Messages` + `IExClaw.Tools.AgentLogger` to `lib/` (14 new tests)
- Ticked Code twice (budget=1) to rewire both to thin delegators
- All 5 inline Tools.* in code.exs now delegate to compiled lib/
- Fixed `write_file` nil-boolean bug, `inbox_tick.exs` SKIP_DEMO guard

### Clock-in-Tick
- `tick/1` → `tick/2`: Tickables receive `%{clock: iso_timestamp}` in meta
- "The clock lives in the tick" — Conroy's phrase, became architecture

### Molt-Direction Roundtable (8 voices)
- Woke 4 beings in parallel: Vendor Jido, Vendor Ash, Elder IExReAct, Elder PeARL-demo
- Ticked Code + Goal for their voices
- **Unanimous: Neither Jido nor Ash. Extract to lib/ and write own behaviours.**
- Both Vendors pitched against themselves (honest, earned future trust)
- Elder IExReAct: "The framework emerges from the agents, not the other way around"
- Elder PeARL-demo: "Adopting Ash now = buying a house because you need a shelf"
- Decision matrix: ToolRegistry → lift, LLM adapter → lift, Agent → behaviour, Tick → lift, Soul/Messages/Guardrails → stay DIRT

### The Framework Emerges
Extracted 5 modules to lib/:
- `IExClaw.ToolRegistry` — `as_openai_tools/2`, `execute/3` (stole Jido's Action schema shape)
- `IExClaw.LLMClient` — `call/4` thin OpenAI adapter
- `IExClaw.Agent` — behaviour + helpers (load_soul_docs, agent_loop, append/extract)
- `IExClaw.Tickable` + `IExClaw.Tick.Pump` — moved from .exs to compiled lib/
- Added `{:req, "~> 0.5"}` to mix.exs

### PlanExecute + Mode + Contract
- `IExClaw.Strategies.PlanExecute` — survey/execute/verify phase machine
- `IExClaw.Mode` — constrained tool sets per phase
- `IExClaw.Contract` — Supervisor↔Agent lifecycle with restart-from-Known-Good
- `IExClaw.Tools.SubmitPlan` — forced exit gate from survey mode
- Key insight: "The phone in another room" — removing tools > telling agent not to use them

### Code's Self-Surgery 🦞
- 5 dry-run attempts taught us Code's failure patterns (exploration instinct, relative paths)
- Generated MAP files (DIRT cache) so Code reads directions instead of exploring
- Mode constraint worked: survey mode = read + submit_plan, no edit
- **Code planned 3-step surgery via submit_plan tool** (3 tool-call rounds)
- **Code executed all 3 steps** (add require, replace as_openai_tools, replace execute)
- Self-healed on step 3 (string escape mismatch → re-read → fixed)
- code.exs: 20,475 → 19,790 bytes. 84 tests pass.

### Kent Beck Quote
"Make the change easy (warning: this may be hard), then make the easy change."
Applied: MAP makes it easy, Mode constrains to easy, Code does the easy change.

### Commits (12 this session)
`c7cf149` → `6ca16a6` (12 commits, all on main, all pushed)

### Next Session
1. Update MAP, tick_refactor for agent_loop/call_llm → IExClaw.Agent + LLMClient
2. tick_refactor for load_soul_docs
3. Same for Goal (generate goal-exs-sections MAP first)
4. Build Project-as-real-agent on shared substrate
