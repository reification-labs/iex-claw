import Config

# Default log level to :info (hides debug noise from Jido.AI)
# Override with LOG_LEVEL=debug at runtime
config :logger, level: :info

# Runtime config handles .env loading - see config/runtime.exs
